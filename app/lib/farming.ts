import fs from "node:fs/promises";import path from "node:path";
export type FarmNode={id:string;campaign:string;campaignType:string;nodeNumber:number;energyCost:number;rate:number;materialId:string};
type Battle={campaign:string;campaignType:string;nodeNumber:number;energyCost:number;rewards?:{potential?:Array<{id:string;effective_rate?:number}>;guaranteed?:Array<{id:string;min?:number;max?:number}>}};
type Recipe={material:string;snowprintId:string;rarity:string;stat:string;craftable:boolean;recipe?:Array<{material:string;count:number}>};
export async function loadFarmingData(){try{const [b,r]=await Promise.all([fs.readFile(path.join(process.cwd(),"data/game/campaign-battles.json"),"utf8"),fs.readFile(path.join(process.cwd(),"data/game/upgrade-recipes.json"),"utf8")]);return {battles:JSON.parse(b) as Record<string,Battle>,recipes:JSON.parse(r) as Record<string,Recipe>};}catch{return null;}}
export function farmNodesFor(materialId:string,battles:Record<string,Battle>):FarmNode[]{const out:FarmNode[]=[];for(const[id,b]of Object.entries(battles)){let rate=0;for(const reward of b.rewards?.guaranteed??[]){if(reward.id===materialId)rate+=(reward.min??1);}for(const reward of b.rewards?.potential??[]){if(reward.id===materialId)rate+=reward.effective_rate??0;}if(rate>0)out.push({id,campaign:b.campaign,campaignType:b.campaignType,nodeNumber:b.nodeNumber,energyCost:b.energyCost,rate,materialId});}return out.sort((a,b)=>(b.rate/b.energyCost)-(a.rate/a.energyCost));}
export function bestFarmNode(materialId:string,battles:Record<string,Battle>){return farmNodesFor(materialId,battles)[0];}
