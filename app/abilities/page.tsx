import Nav from "../components/Nav";import AbilityTable from "./AbilityTable";import {getReport,unitFor} from "../lib/report";
export default async function Abilities(){const r=await getReport();if(!r)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;
const rows=r.abilityQueue.map(x=>{const u=unitFor(r,x.character);return {...x,id:u?.id,icon:u?.icon}});
return <main><Nav/><header><div><p className="eyebrow">ABILITIES</p><h1>Uncommon Badge Queue</h1><p className="sub">Characters with an active or passive below your level-17 baseline.</p></div><div className="power">{r.summary.individualAbilityUpgradesTo17} <strong>upgrades</strong></div></header><section className="panel tablePanel"><AbilityTable rows={rows}/></section></main>}