import Nav from "../components/Nav"; import CharacterName from "../components/CharacterName"; import {getReport,unitFor} from "../lib/report";
export default async function Abilities(){const r=await getReport();if(!r)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;
return <main><Nav/><header><div><p className="eyebrow">ABILITIES</p><h1>Uncommon Badge Queue</h1><p className="sub">Characters with an active or passive below your level-17 baseline.</p></div><div className="power">{r.summary.individualAbilityUpgradesTo17} <strong>upgrades</strong></div></header>
<section className="panel"><div className="tableWrap"><table><thead><tr><th>Character</th><th>Active</th><th>Passive</th><th>Priority</th><th>Focus</th><th>Basis</th></tr></thead><tbody>
{r.abilityQueue.map(x=>{const u=unitFor(r,x.character);return <tr key={x.character}><td><CharacterName name={x.character} id={u?.id} icon={u?.icon}/><small>{x.faction}</small></td><td><strong className={x.activeTo17?"below":""}>{x.activeLevel}</strong><small>{x.activeId}</small></td><td><strong className={x.passiveTo17?"below":""}>{x.passiveLevel}</strong><small>{x.passiveId}</small></td><td>{x.accountPriority}</td><td>{x.focus}</td><td><small>{x.basis}</small></td></tr>})}
</tbody></table></div></section></main>}