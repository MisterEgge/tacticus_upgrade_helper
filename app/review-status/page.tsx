import Nav from"../components/Nav";import CharacterName from"../components/CharacterName";import{getAbilityBreakpoints,getCampaignTargets,getCharacterCatalog}from"../lib/catalog";import{getReport}from"../lib/report";import{readFile}from"node:fs/promises";import{formatAbilityName}from"../../src/domain/abilities";import{abilityResearchRows,hasCharacterSpecificAbilityGuidance}from"../../src/domain/abilityReview";
export default async function ReviewStatus(){const[catalog,abilities,campaigns,sourceText,report,priorityText]=await Promise.all([getCharacterCatalog(),getAbilityBreakpoints(),getCampaignTargets(),readFile("config/ability_breakpoint_sources.json","utf8"),getReport(),readFile("config/character_priorities.json","utf8")]);const sources=JSON.parse(sourceText)as Record<string,unknown>;const priorities=JSON.parse(priorityText)as Record<string,{modes?:string[]}>;const reviewedAbilities=catalog.characters.filter(character=>hasCharacterSpecificAbilityGuidance(abilities[character.name]));const sourcedAbilities=reviewedAbilities.filter(character=>Array.isArray(sources[character.name])&&(sources[character.name]as unknown[]).length>0);const rows=report?abilityResearchRows(catalog.characters,report.roster,abilities,sources,priorities):[];const targetBacklog=rows.filter(row=>row.status==="Targets needed");const evidenceBacklog=rows.filter(row=>row.status==="Evidence needed");const campaignRows=Object.entries(campaigns.campaigns).flatMap(([campaign,value])=>Object.entries(value.characters).map(([character,target])=>({campaign,character,reviewed:Boolean(target.rank&&target.evidence?.length)})));const reviewedCampaigns=campaignRows.filter(row=>row.reviewed);const pendingCampaigns=campaignRows.filter(row=>!row.reviewed);return <main><Nav/><header><div><p className="eyebrow">DATA QUALITY</p><h1>Review Coverage</h1><p className="sub">The ability backlog is account-specific. It shows the exact character data we have, then makes missing targets and missing evidence explicit.</p></div></header><section className="cards compact"><div className="card"><strong>{reviewedAbilities.length}/{catalog.characters.length}</strong><span>Ability reviews</span><small>{targetBacklog.length} owned characters still need targets</small></div><div className="card"><strong>{sourcedAbilities.length}/{reviewedAbilities.length}</strong><span>Ability reviews with evidence</span><small>{evidenceBacklog.length} owned entries still need a source record</small></div><div className="card"><strong>{reviewedCampaigns.length}/{campaignRows.length}</strong><span>Campaign character targets</span><small>{pendingCampaigns.length} target rows still need research</small></div></section>{report?<section className="panel detailPanel"><div className="sectionTitle"><div><p className="eyebrow">ABILITY BACKLOG</p><h2>Owned characters needing ability research</h2><p className="sub">Targets needed means no character-specific stopping points exist yet. Evidence needed means targets exist but need a durable source link.</p></div><div className="power">{targetBacklog.length}<strong> targets needed</strong></div></div><div className="tableWrap"><table><thead><tr><th>Character</th><th>Active</th><th>Passive</th><th>Relevant account goals</th><th>Research status</th></tr></thead><tbody>{rows.filter(row=>row.status!=="Complete").map(row=><tr key={row.id}><td><CharacterName name={row.name} id={row.id}/></td><td>{formatAbilityName(row.active)}<small>Current: {row.activeLevel??"UNKNOWN"}</small></td><td>{formatAbilityName(row.passive)}<small>Current: {row.passiveLevel??"UNKNOWN"}</small></td><td>{[...row.campaigns,...row.modes].join(" · ")||"No linked goal recorded"}</td><td className="below">{row.status}<small>{row.status==="Targets needed"?"Need practical targets, high-investment range, and role notes":"Need an auditable source record"}</small></td></tr>)}</tbody></table></div></section>:<section className="empty">Run <code>npm run refresh</code> to match ability research to your account roster.</section>}<section className="panel detailPanel"><div className="sectionTitle"><div><p className="eyebrow">CAMPAIGN BACKLOG</p><h2>Mandatory targets still needing research</h2></div></div><p className="sub">{pendingCampaigns.map(row=>`${row.campaign}: ${row.character}`).join(" · ")||"None"}</p></section></main>}
