import{readFile}from"node:fs/promises";import Nav from"../components/Nav";import WarDefensePlanner from"./WarDefensePlanner";import{getCharacterCatalog}from"../lib/catalog";import{getReport}from"../lib/report";
type Team={name:string;core:string[];flex:Array<{name:string;used:number}>};type Plan={teams:Team[]};
export default async function WarDefense(){const[report,catalog,planText]=await Promise.all([getReport(),getCharacterCatalog(),readFile("config/war_defense_teams.json","utf8")]);if(!report)return <main><Nav/><div className="empty">Run <code>npm run refresh</code> to load your roster.</div></main>;const plan=JSON.parse(planText)as Plan;const units=new Map(report.roster.map(unit=>[unit.name,unit]));const icons=new Map(catalog.characters.map(character=>[character.id,character.icon]));const needs=[...report.equipmentAllocation.equipNow,...report.equipmentAllocation.buyWatch,...report.equipmentAllocation.compatibilityUnknown];const assigned=new Set<string>();const teams=plan.teams.flatMap(team=>{if(!team.core.every(name=>units.has(name)&&!assigned.has(name)))return[];const flex=team.flex.filter(unit=>units.has(unit.name)&&!assigned.has(unit.name)&&!team.core.includes(unit.name)).sort((a,b)=>b.used-a.used).slice(0,2).map(unit=>unit.name);if(flex.length<2)return[];const members=[...team.core,...flex].map(name=>{const unit=units.get(name)!;return{id:unit.id,name,icon:icons.get(unit.id),rank:unit.rank,activeLevel:unit.abilities[0]?.level??null,passiveLevel:unit.abilities[1]?.level??null,gear:needs.filter(row=>row.character===name).length};});members.forEach(member=>assigned.add(member.name));return[{name:team.name,members}];}).slice(0,5);return <main><Nav/><header><div><p className="eyebrow">GUILD WAR</p><h1>War Defense Build Plan</h1><p className="sub">Set each distinct team to Gold I / 35–35 or Silver I / 26–26. Slots 1–2 default Gold; the rest default Silver.</p></div><div className="power">{teams.length}<strong> defense slots</strong></div></header><WarDefensePlanner teams={teams}/></main>}
