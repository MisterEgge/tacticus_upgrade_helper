"use client";
import { useState } from "react";
import Link from "next/link";
import CharacterName from "../components/CharacterName";
import { rankName } from "../../src/domain/ranks";
import { warGearLabel } from "../../src/domain/warGear";
type Member={id:string;name:string;icon:string|undefined;rank:number;activeLevel:number|null;passiveLevel:number|null;items:Array<{slotId:string;rarity?:string;level:number;name?:string}>};type Team={name:string;used:number;wins:number;members:Member[]};
const targets={gold:{label:"Gold I · 35 / 35",rank:12,ability:35},silver:{label:"Silver I · 26 / 26",rank:9,ability:26}} as const;
export default function WarDefensePlanner({teams,offenseTeams}:{teams:Team[];offenseTeams:Team[]}){
 const[mode,setMode]=useState<"defense"|"offense">("defense"),[slots,setSlots]=useState([0,1,2,3,4]),[open,setOpen]=useState<number|null>(null);const options=mode==="defense"?teams:offenseTeams;const assigned=slots.map(i=>teams[i]).filter((x):x is Team=>!!x);const dup=assigned.flatMap(t=>t.members.map(m=>m.name)).filter((n,i,a)=>a.indexOf(n)!==i);
 const detail=(team:Team,target:keyof typeof targets)=><div className="tableWrap"><table><thead><tr><th>Character</th><th>Rank</th><th>Abilities</th><th>Gear</th></tr></thead><tbody>{team.members.map(m=>{const t=targets[target],r=Math.max(0,t.rank-m.rank),a=Math.max(0,t.ability-(m.activeLevel??0)),p=Math.max(0,t.ability-(m.passiveLevel??0)),gear=warGearLabel(m.items);return <tr key={m.id}><td><Link className="characterLink" href={`/characters/${m.id}`}><CharacterName name={m.name} id={m.id} icon={m.icon}/></Link></td><td className={r?"below":"ready"}>{rankName(m.rank)}<small>{r?`+${r} → ${rankName(t.rank)}`:"Met"}</small></td><td className={a||p?"below":"ready"}>{m.activeLevel??"?"} / {m.passiveLevel??"?"}<small>{a||p?`+${a} active · +${p} passive`:"Met"}</small></td><td className={gear.ready?"ready":"below"}><strong>{gear.summary}</strong><small>{gear.detail}</small></td></tr>})}</tbody></table></div>;
 return <><div className="abilityViews warModeTabs"><button className={mode==="defense"?"active":""} onClick={()=>setMode("defense")}>Defense options</button><button className={mode==="offense"?"active":""} onClick={()=>setMode("offense")}>Offense options</button></div>{mode==="defense"?<section className="panel detailPanel"><div className="sectionTitle"><div><p className="eyebrow">YOUR FIVE DEFENSE SLOTS</p><h2>Select a team, then inspect it in place</h2><p className="sub">Slots 1–2 target Gold I / 35–35; slots 3–5 target Silver I / 26–26.</p></div><div className="power">{new Set(dup).size?<strong className="below">{new Set(dup).size} conflicts</strong>:<strong className="ready">No conflicts</strong>}</div></div>{slots.map((choice,i)=>{const team=teams[choice]!,target=i<2?"gold":"silver",conflicts=team.members.filter(m=>dup.includes(m.name)).map(m=>m.name);return <div className="campaignInvestmentGroup" key={i}><button className="campaignSectionToggle" onClick={()=>setOpen(open===i?null:i)}><div><p className="eyebrow">DEFENSE {i+1} · {targets[target].label}</p><h2>{team.name}</h2><p className={conflicts.length?"warSource below":"warSource"}>{conflicts.length?`Duplicate: ${conflicts.join(" · ")}`:"Distinct team"}</p></div><div className="campaignSectionStatus"><select value={choice} onClick={event=>event.stopPropagation()} onChange={event=>setSlots(current=>current.map((v,index)=>index===i?Number(event.target.value):v))}>{teams.map((option,index)=><option key={option.name} value={index}>{option.name}</option>)}</select><span className="campaignChevron">{open===i?"▴":"▾"}</span></div></button>{open===i?<div className="campaignSectionBody">{detail(team,target)}</div>:null}</div>})}</section>:null}<section className="panel detailPanel"><div className="sectionTitle"><div><p className="eyebrow">{mode.toUpperCase()} SOURCE OPTIONS</p><h2>Compare alternatives</h2></div><div className="power">{options.length}<strong> options</strong></div></div>{options.map((team,i)=><div className="campaignInvestmentGroup" key={team.name}><h3>{i+1}. {team.name} · {team.used.toLocaleString()} uses · {(team.wins/team.used*100).toFixed(1)}% win</h3></div>)}</section></>;
}
