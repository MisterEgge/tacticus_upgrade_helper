import Nav from "../components/Nav";import{getReport}from"../lib/report";import CharacterTable from "./CharacterTable";
export default async function Characters(){const r=await getReport();if(!r)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;return <main><Nav/><header><div><p className="eyebrow">ROSTER</p><h1>Characters</h1><p className="sub">Search and sort your complete live roster.</p></div><div className="power">{r.roster.length} <strong>units</strong></div></header><section className="panel"><CharacterTable rows={r.roster}/></section></main>}