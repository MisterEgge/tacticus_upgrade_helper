import Nav from "../components/Nav";import{getReport}from"../lib/report";import CharacterTable from "./CharacterTable";
export default async function Characters(){const r=await getReport();if(!r)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;const gear=[...r.equipmentAllocation.equipNow,...r.equipmentAllocation.buyWatch];const review=r.equipmentAllocation.compatibilityUnknown;const ability=new Map(r.abilityQueue.map(x=>[x.character,Number(x.activeTo17)+Number(x.passiveTo17)]));const rows=r.roster.map(unit=>({...unit,gearTargets:gear.filter(x=>x.character===unit.name).length,gearReview:review.some(x=>x.character===unit.name),abilityGaps:ability.get(unit.name)??0}));return <main><Nav/><header><div><p className="eyebrow">ROSTER</p><h1>Characters</h1><p className="sub">Search your live roster and see each character’s current upgrade work at a glance.</p></div><div className="power">{r.roster.length} <strong>units</strong></div></header><section className="panel"><CharacterTable rows={rows}/></section></main>}
