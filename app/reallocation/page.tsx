import {readFile}from "node:fs/promises";import Link from "next/link";import Nav from "../components/Nav";import {getCharacterCatalog}from "../lib/catalog";import {getReport}from "../lib/report";import {legendaryReallocationAudit}from "../../src/domain/reallocation";import {campaignIsComplete,requiredCampaignName,type CampaignBattleDefinition}from "../../src/domain/campaigns";
type Focus=Record<string,{modes?:string[]}>;
export default async function ReallocationPage(){const[report,catalog,focus,battleText]=await Promise.all([getReport(),getCharacterCatalog(),readFile("config/character_priorities.json","utf8").then(value=>JSON.parse(value)as Focus),readFile("data/game/campaign-battles.json","utf8")]);if(!report)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;const needs=report.equipmentAllocation.buyWatch.flatMap(row=>{const ids=row.preferredLegendaryItemIds??[];return ids.length===1&&row.characterId?[{character:row.character,characterId:row.characterId,slotId:row.slotId,itemId:ids[0]!,itemName:row.preferredLegendaryItems?.[0]??ids[0]!}]:[]});const battles=Object.values(JSON.parse(battleText))as CampaignBattleDefinition[];const incomplete=new Set((report.campaignProgress??[]).filter(progress=>!campaignIsComplete(progress,battles)).map(requiredCampaignName));const campaignRequired=new Set(catalog.characters.filter(character=>character.campaignsRequiredIn.some(name=>incomplete.has(name))).map(character=>character.name));const rows=legendaryReallocationAudit(needs,report.roster,focus,campaignRequired).filter(row=>row.donors.length);return <main><Nav/><header><div><p className="eyebrow">LEGENDARY GEAR</p><h1>Reallocation Audit</h1><p className="sub">Exact Legendary matches that could help a current gear target. This is deliberately conservative: it never marks an equipped item as safe to take.</p></div><div className="power">{rows.length}<strong> matches</strong></div></header><section className="panel tablePanel"><p className="sub">Characters required by incomplete campaigns, current Raid/War/LRE cores, and characters with their own Legendary needs stay protected. Others remain manual decisions.</p>{!rows.length?<div className="empty">No exact Legendary duplicates currently match a one-item target.</div>:<div className="tableWrap"><table><thead><tr><th>Build target</th><th>Equipped match</th><th>Why it stays protected</th></tr></thead><tbody>{rows.flatMap(row=>row.donors.map(donor=><tr key={`${row.characterId}:${row.slotId}:${donor.characterId}:${donor.slotId}`}><td><Link className="sourceLink" href={`/characters/${row.characterId}`}>{row.character}</Link><small>{row.slotId} · {row.itemName}</small></td><td><Link className="sourceLink" href={`/characters/${donor.characterId}`}>{donor.character}</Link><small>{donor.slotId} · level {donor.level}</small></td><td>{donor.protection}</td></tr>))}</tbody></table></div>}</section></main>}
