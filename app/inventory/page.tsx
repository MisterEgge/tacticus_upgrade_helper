import Nav from "../components/Nav";import{getReport}from"../lib/report";import InventoryTable from "./InventoryTable";
export default async function Inventory(){const r=await getReport();if(!r)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;return <main><Nav/><header><div><p className="eyebrow">INVENTORY</p><h1>Unequipped Inventory</h1><p className="sub">Search and sort everything currently returned by the player API.</p></div><div className="power">{r.unequippedInventory.reduce((n,x)=>n+x.amount,0)} <strong>items</strong></div></header><section className="panel"><InventoryTable rows={r.unequippedInventory}/></section></main>}