import Nav from "../components/Nav";import{getReport,targetName}from"../lib/report";import InventoryTable from "./InventoryTable";
export default async function Inventory(){const r=await getReport();if(!r)return <main><Nav/><div className="empty">Run <code>npm run refresh</code>.</div></main>;
const needs=[...r.equipmentAllocation.equipNow,...r.equipmentAllocation.buyWatch].map(x=>({item:targetName(x),character:x.character,priority:x.accountPriority}));
const rows=r.unequippedInventory.map(i=>{const demand=needs.filter(n=>n.item===(i.name??i.id)).sort((a,b)=>b.priority-a.priority);return {...i,demandCount:demand.length,topRecipient:demand[0]?.character??"",topPriority:demand[0]?.priority??0}});
return <main><Nav/><header><div><p className="eyebrow">INVENTORY</p><h1>Unequipped Inventory</h1><p className="sub">What you own, plus current demand from the Legendary equipment planner.</p></div><div className="power">{r.unequippedInventory.reduce((n,x)=>n+x.amount,0)} <strong>items</strong></div></header><section className="panel tablePanel"><InventoryTable rows={rows}/></section></main>}