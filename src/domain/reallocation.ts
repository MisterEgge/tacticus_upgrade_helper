export type ReallocationNeed={character:string;characterId:string;slotId:string;itemId:string;itemName:string};
export type ReallocationUnit={id:string;name:string;items:Array<{slotId:string;id:string;level:number}>};
export type ReallocationProtection="Campaign-required character — keep protected"|"Has unmet Legendary gear targets — keep protected"|`Core account mode: ${string} — keep protected`|"Role has not been reviewed — manual decision required";
export type ReallocationDonor={character:string;characterId:string;slotId:string;level:number;protection:ReallocationProtection};
export type ReallocationResult=ReallocationNeed&{donors:ReallocationDonor[]};
export function legendaryReallocationAudit(needs:ReallocationNeed[],units:ReallocationUnit[],focus:Record<string,{modes?:string[]}>,campaignRequired:Set<string>):ReallocationResult[]{const needy=new Set(needs.map(need=>need.character));const protectionFor=(unit:ReallocationUnit):ReallocationProtection=>{if(campaignRequired.has(unit.name))return "Campaign-required character — keep protected";if(needy.has(unit.name))return "Has unmet Legendary gear targets — keep protected";const modes=focus[unit.name]?.modes??[];return modes.length?`Core account mode: ${modes.join(" / ")} — keep protected`:"Role has not been reviewed — manual decision required";};return needs.map(need=>({...need,donors:units.flatMap(unit=>unit.items.filter(item=>item.id===need.itemId&&unit.name!==need.character).map(item=>({character:unit.name,characterId:unit.id,slotId:item.slotId,level:item.level,protection:protectionFor(unit)})))}));}
